#include<iostream>
 
using namespace std;

string diff(string a, string b){
	int n = a.length(), m = b.length();
	int 
}
 
int main(){
	ios::sync_with_stdio(false);
	string a, b;
	cin >> a >> b;
	cout << diff(a,b);
	return 0;
}